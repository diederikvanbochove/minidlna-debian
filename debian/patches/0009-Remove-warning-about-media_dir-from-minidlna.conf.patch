From: Benoît Knecht <benoit.knecht@fsfe.org>
Subject: Remove warning about media_dir from minidlna.conf

Changes to media_dir are now detected and the library is rescanned
automatically, so the warning is irrelevant.
---
 minidlna.conf | 5 -----
 1 file changed, 5 deletions(-)

diff --git a/minidlna.conf b/minidlna.conf
index ad3f708..42cb72a 100644
--- a/minidlna.conf
+++ b/minidlna.conf
@@ -21,11 +21,6 @@
 #   * "A" for audio    (eg. media_dir=A,/var/lib/minidlna/music)
 #   * "P" for pictures (eg. media_dir=P,/var/lib/minidlna/pictures)
 #   * "V" for video    (eg. media_dir=V,/var/lib/minidlna/videos)
-#
-# WARNING: After changing this option, you need to rebuild the database. Either
-#          run minidlna with the '-R' option, or delete the 'files.db' file
-#          from the db_dir directory (see below).
-#          On Debian, you can run, as root, 'service minidlna force-reload' instead.
 media_dir=/var/lib/minidlna
 
 # Path to the directory that should hold the database and album art cache.
